using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class extraPlateHandler : MonoBehaviour
{
    public Text item;

    // Start is called before the first frame update
    void Start()
    {
        item.text = "";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
